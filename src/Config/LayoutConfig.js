import Layout from "Layouts/Default/Layout";

/**
 * config file for Layout
 */
export const LayoutConfig = {

  /**
   * you can change the render layout
   * within component use context
   */
  default: Layout,
  /**
   * set the default layout folder name
   */
  defaultLayoutFolderName : 'Default',

};